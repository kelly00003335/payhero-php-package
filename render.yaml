
services:
  - type: web
    name: payhero-payment-gateway
    env: php
    buildCommand: composer install
    startCommand: php -S 0.0.0.0:$PORT index.php
    envVars:
      - key: PORT
        value: 8080
